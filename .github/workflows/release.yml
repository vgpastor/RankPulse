name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-web:
    name: Build SPA bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 10.33.2 }
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build web SPA
        run: pnpm --filter @rankpulse/web build
        env:
          VITE_API_BASE_URL: ${{ vars.PUBLIC_API_BASE_URL }}
      - name: Pack dist for upload
        run: tar czf web-dist.tar.gz -C apps/web/dist .
      - uses: actions/upload-artifact@v4
        with: { name: web-dist, path: web-dist.tar.gz, retention-days: 7 }

  deploy:
    name: Deploy to srv07 (PM2 + Plesk Docker)
    needs: build-web
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/download-artifact@v4
        with: { name: web-dist }

      - name: Upload web bundle to srv07
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SRV07_HOST }}
          username: ${{ secrets.SRV07_USER }}
          key: ${{ secrets.SRV07_SSH_KEY }}
          source: web-dist.tar.gz
          target: /var/www/vhosts/ingenierosweb.co/rankpulse.ingenierosweb.co/

      - name: Deploy code + reload PM2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SRV07_HOST }}
          username: ${{ secrets.SRV07_USER }}
          key: ${{ secrets.SRV07_SSH_KEY }}
          script_stop: true
          envs: GITHUB_SHA
          script: |
            set -euo pipefail
            VHOST=/var/www/vhosts/ingenierosweb.co/rankpulse.ingenierosweb.co
            APP=$VHOST/app
            export PATH=$HOME/.fnm:$PATH
            eval "$(fnm env --shell bash)"
            fnm use 24
            cd "$APP"
            echo ">>> Pull main"
            git fetch --quiet origin main
            git reset --hard origin/main
            echo ">>> pnpm install"
            pnpm install --frozen-lockfile
            echo ">>> Drizzle migrate (loads .env + .env.local via Node --env-file)"
            pnpm --filter @rankpulse/infrastructure db:migrate
            echo ">>> Replace web bundle in httpdocs"
            rm -rf $VHOST/httpdocs/*
            tar xzf $VHOST/web-dist.tar.gz -C $VHOST/httpdocs/
            rm -f $VHOST/web-dist.tar.gz
            echo ">>> Reload PM2"
            pm2 reload $APP/ops/ecosystem.config.cjs --update-env
            echo ">>> Health check"
            sleep 5
            curl --fail --silent --show-error --max-time 5 --retry 8 --retry-delay 2 --retry-all-errors http://127.0.0.1:3200/healthz
            echo ""
            echo ">>> Deploy OK (sha-${GITHUB_SHA::7})"
