name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    steps:
      - name: Trivial
        run: echo "hello from CI on $RUNNER_OS"
