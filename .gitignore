node_modules/
dist/
build/
coverage/
.turbo/
.nx/cache
.nx/workspace-data
*.log
.DS_Store
# .env is committed (dev defaults, safe values only — no secrets).
# .env.local is for real secrets and is NEVER committed.
.env.local
.env.*.local
*.tsbuildinfo
.vscode/
.idea/
.pnpm-store/
.claude/
