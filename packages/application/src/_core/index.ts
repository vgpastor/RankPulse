// Internal application-layer infrastructure for the manifest/module pattern (ADR 0002).
// Populated by Tasks 1.5 (ContextModule types) and 1.6 (buildAutoScheduleHandlers).
// Re-exported from `packages/application/src/index.ts` so consumers import via
// `@rankpulse/application` rather than introducing a new workspace package.
export {};
